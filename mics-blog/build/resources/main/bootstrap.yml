spring:
  application:
    name: shengnianos-consul-blog
  cloud:
    consul:
      host: sos-consul-server
      port: 8500
      enabled: true
      config:
        watch:
          enabled: true
          format: YAML
