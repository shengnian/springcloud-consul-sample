spring:
  application:
    name: shengnianos-consul-client
  cloud:
    consul:
      host: sos-consul-server
      port: 8500
      config:
        watch:
          enabled: true
          format: YAML